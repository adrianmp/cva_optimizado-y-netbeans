$(document).ready(function()
{
		//window.location.href = "index.php"; //this is a possibility
		//window.location.reload(); //another possiblity
		//history.go(0); //and another
		
});

function insertado(id)
{
	if(id)
		parent.$.fancybox.close();
}
